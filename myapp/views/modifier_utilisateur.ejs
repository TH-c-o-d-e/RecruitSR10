<!DOCTYPE html>
<html lang="fr">
<head>
    <title>Modifier Utilisateur</title>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="test.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <header class="text-center"><h1>Modifier Utilisateur</h1></header>
        </div>

        <div class="row">
            <nav class="nav nav-pills navbar-expand-lg navbar-light bg-light mt-2 mb-2">
                <a class="nav-item nav-link mx-2" href="./">Accueil</a>
                <a class="nav-item nav-link mx-2" href="./gestion_utilisateurs">Retour à la gestion des utilisateurs</a>
            </nav>
        </div>

        <div class="row">
            <div class="col-sm-12">
                <form action="/users/modifier_utilisateur/<%= user.id %>" method="POST">
                    <div class="mb-3">
                        <label for="nom" class="form-label">Nom</label>
                        <input type="text" class="form-control" id="nom" name="nom" value="<%= user.nom %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="prenom" class="form-label">Prénom</label>
                        <input type="text" class="form-control" id="prenom" name="prenom" value="<%= user.prenom %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" name="email" value="<%= user.email %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="coordonnees" class="form-label">Coordonnées</label>
                        <textarea class="form-control" id="coordonnees" name="coordonnees"><%= user.coordonnees %></textarea>
                    </div>
                    <div class="mb-3">
                        <label for="role" class="form-label">Rôle</label>
                        <select class="form-select" id="role" name="role" required>
                            <option value="utilisateur" <%= user.role === '1' ? 'selected' : '' %>1</option>
                            <option value="administrateur" <%= user.role === '0' ? 'selected' : '' %>0</option>
                            <option value="recruteur" <%= user.role === '2' ? 'selected' : '' %>2</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary">Modifier</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
