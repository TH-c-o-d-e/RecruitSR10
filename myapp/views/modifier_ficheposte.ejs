<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>Modifier Fiche de Poste</title>
    <link rel="stylesheet" href="test.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5">
      <header class="row text-center">
        <h1>Recrutement</h1>
      </header>

      <nav
        class="nav nav-pills navbar-expand-lg navbar-light bg-light mt-2 mb-2"
      >
        <a class="nav-item nav-link mx-2" href="accueil_recruteur.html"
          >Accueil</a
        >
        <a class="nav-item nav-link mx-2" href="gestion_offres.html"
          >Gestion des offres</a
        >
        <a class="nav-item nav-link active mx-2" href="gestion_ficheposte.html"
          >Gestion des fiches de poste</a
        >
      </nav>

      <div class="row">
        <div class="col-sm-8">
          <h1>Modification de fiche de poste</h1>
          <form
            action="/ficheposte/modifier/<%= ficheposte.id %>"
            method="POST"
          >
            <div class="mb-4 mt-4">
              <label for="intitule" class="form-label">Intitulé : </label>
              <input
                type="text"
                class="form-control"
                id="intitule"
                name="intitule"
                value="<%= ficheposte.intitule %>"
                required="required"
              />
            </div>
            <div class="mb-4 mt-4">
              <label for="statut" class="form-label">Statut de poste : </label>
              <input
                type="text"
                class="form-control"
                id="statut"
                name="statut"
                value="<%= ficheposte.statut %>"
                required="required"
              />
            </div>
            <div class="mb-4 mt-4">
              <label for="responsable" class="form-label"
                >Responsable hiérarchique :
              </label>
              <input
                type="text"
                class="form-control"
                id="responsable"
                name="responsable"
                value="<%= ficheposte.responsable %>"
                required="required"
              />
            </div>
            <div class="mb-4 mt-4">
              <label for="lieu" class="form-label">Lieu de mission : </label>
              <input
                type="text"
                class="form-control"
                id="lieu"
                name="lieu"
                value="<%= ficheposte.lieu %>"
                required="required"
              />
            </div>
            <div class="mb-4 mt-4">
              <label for="rythme" class="form-label">Rythme : </label>
              <input
                type="text"
                class="form-control"
                id="rythme"
                name="rythme"
                value="<%= ficheposte.rythme %>"
                required="required"
              />
            </div>
            <div class="mb-4 mt-4">
              <label for="salaire" class="form-label"
                >Fourchette de salaire :
              </label>
              <input
                type="text"
                class="form-control"
                id="salaire"
                name="salaire"
                value="<%= ficheposte.salaire %>"
                required="required"
              />
            </div>
            <div class="mt-4 mb-4">
              <button type="submit" class="btn btn-primary">
                Enregistrer les modifications
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <footer></footer>
  </body>
</html>
