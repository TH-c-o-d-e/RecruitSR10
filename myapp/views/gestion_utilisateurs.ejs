<!DOCTYPE html>
<html lang="fr">
<head>
    <title>Gestion des Utilisateurs</title>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="test.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <% if (error) { %>
    <div class="alert alert-danger" role="alert">
        <%= error %>
    </div>
    <% } %>

    <div class="container mt-5">
        <div class="row">
            <header class="text-center"><h1>Gestion des Utilisateurs</h1></header>
        </div>

        <div class="row">
            <nav class="nav nav-pills navbar-expand-lg navbar-light bg-light mt-2 mb-2">
                <a class="nav-item nav-link mx-2" href="./">Accueil</a>
                <a class="nav-item nav-link active mx-2" href="./gestion_utilisateurs">Gestion des utilisateurs</a>
                <a class="nav-item nav-link mx-2" href="./gestion_organisations">Gestion des organisations</a>
            </nav>
        </div>

        <div class="row">
            <div class="col-sm-12">
                <form action="/gestion_utilisateurs/gerer_utilisateur" method="POST">
                    <button type="submit" value="suppression" name="choix" class="btn btn-danger">Supprimer</button>
                    <button type="submit" value="modification" name="choix" class="btn btn-primary">Modifier</button>
                    <button type="submit" value="droits_admin" name="choix" class="btn btn-primary">Accorder droits d'administration</button>
                    <table class="table">
                        <thead>
                            <tr>
                                <th><input type="radio" disabled="true" /></th>
                                <th>Nom</th>
                                <th>Prénom</th>
                                <th>Email</th>
                                <th>Type de Compte</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (let i = 0; i < user.length; i++) { %>
                            <tr>
                                <td>
                                    <input name="utilisateur" value="<%= user[i].id %>" type="radio" required />
                                </td>
                                <td><%= user[i].nom %></td>
                                <td><%= user[i].prenom %></td>
                                <td><%= user[i].email %></td>
                                <td><%= user[i].type === 0 ? 'Administrateur' : 'Utilisateur standard' %></td>
                            </tr>
                            <% } %>
                        </tbody>
                    </table>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
