<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8" />
    <title>Gestion des fiches de poste</title>
    <link rel="stylesheet" href="test.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="container mt-5">
        <header class="row text-center">
            <h1>Gestion des fiches de poste</h1>
        </header>

        <nav class="nav nav-pills navbar-expand-lg navbar-light bg-light mt-2 mb-2">
            <a class="nav-item nav-link mx-2" href="/accueil_recruteur">Accueil</a>
            <a class="nav-item nav-link mx-2" href="/ficheposte/gestion_fiches_de_poste">Gestion des fiches de poste</a>
            <a class="nav-item nav-link active mx-2" href="/ficheposte/nouvelle">Nouvelle fiche de poste</a>
        </nav>

        <div class="row mb-3">
            <div class="col-sm-12 d-flex justify-content-end">
                <a href="/ficheposte/nouvelle" class="btn btn-success mx-2" id="new-post">Nouvelle fiche de poste</a>
            </div>
        </div>

        <div class="row">
            <div class="col-sm-12">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th scope="col">ID</th>
                            <th scope="col">Intitulé</th>
                            <th scope="col">Statut</th>
                            <th scope="col">Responsable</th>
                            <th scope="col">Lieu</th>
                            <th scope="col">Rythme</th>
                            <th scope="col">Salaire</th>
                            <th scope="col">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% fiche.forEach(ficheposte => { %>
                            <tr>
                                <td><%= ficheposte.id %></td>
                                <td><%= ficheposte.intitule %></td>
                                <td><%= ficheposte.statut %></td>
                                <td><%= ficheposte.responsable %></td>
                                <td><%= ficheposte.lieu %></td>
                                <td><%= ficheposte.rythme %></td>
                                <td><%= ficheposte.salaire %></td>
                                <td>
                                    <a href="/ficheposte/modifier/<%= ficheposte.id %>" class="btn btn-primary btn-sm mx-1">Modifier</a>
                                    <form action="/ficheposte/supprimer/<%= ficheposte.id %>" method="POST" style="display: inline;">
                                        <button type="submit" class="btn btn-danger btn-sm mx-1">Supprimer</button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <footer></footer>
</body>
</html>
